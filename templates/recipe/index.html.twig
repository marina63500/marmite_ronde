{% extends 'base.html.twig' %}

{% block title %}Hello RecipeController!
{% endblock %}

{% block body %}
<style>
	.example-wrapper {
		margin: 1em auto;
		max-width: 800px;
		width: 95%;
		font: 18px / 1.5 sans-serif;
	}
	.example-wrapper code {
		background: #F5F5F5;
		padding: 2px 6px;
	}
</style>


<div class="example-wrapper">
	<h1>Recettes de Marmite Ronde</h1>

	{#boucle pour afficher une recette par cards#}

	<div
		class="row g-5">

		{# {% for recipe in recipes %} #}
		<div class="col-md-4 col-lg-4 col-sm-6">
			<div
				class="card ">
				{# <img src="{{recipe.media_id}}" class="card-img-top" alt="photo de la recette." > #}
				<div class="card-body">
					<h5 class="card-title">title :
						{# {{recipe.title}} #}
					</h5>
					<p class="card-text">durée :
						{# {{recipe.duration}} #}
					</p>

					<p class="card-text">Auteur :
						{# {{recipe.user_id}} #}
					</p>
					<a href="" class="btn btn-warning">détail</a>					
					{# <a href="{{path('recipe_show',{'id' : recipe.id})}}" class="btn btn-warning">détail</a>
					 #}
				</div>
			</div>
		</div>
	</div>
</div>
{# {% endfor %} #}
{% endblock %}
