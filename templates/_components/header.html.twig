<header>

  {# menu de navigation #}
	<nav class="navbar navbar-expand-lg bg-body-tertiary">
	
		<div class="container-fluid">
			<a class="navbar-brand" href="#">Marmite ronde</a>
      {# <img src="assets/images/logo_site_cuisine" alt="logo_site_cuisine" class="logo"> #}
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a class="nav-link active" aria-current="page" href="{{path('home_index')}}">Accueil</a>
					</li>
					<li class="nav-item">
						<a class="nav-link " aria-current="page" href="{{path('recipe_index')}}">Recettes</a>
					</li>
          {# menu choix de la difficulté #}
					<li class="nav-item dropdown">
						{# <a class="nav-link dropdown-toggle" href="{{path('recipe_difficult',{id : difficult.id})}}" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Difficultés
						</a> #}
						<ul class="dropdown-menu">
							<li>
								<a class="dropdown-item" href="#">Facile</a>
							</li>
							<li>
								<a class="dropdown-item" href="#">Moyen</a>
							</li>
							<li>
								<a class="dropdown-item" href="#">Difficile</a>
							</li>
						</ul>
					</li>

          {# pour se connecter/déconnecter #}
					{# <li class="nav-item"></li> #}
					<li class="nav-item">
						{% if app.user %}
							<p class="nav-link mb-0">bienvenue ,
								{{app.user.email}}</p>

							<a class="nav-link" href="{{path('app_logout')}}">Se déconnecter</a>
						{% else %}
							<li class="nav-item">
								<a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{path('app_register')}}">S'enregistrer</a>
							</li>
						{% endif %}
					</li>

				</ul>
			</div>
		</div>
	</nav>

</header>
